# Runtime dependencies for API deployment (Render free-tier friendly)
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
flask>=3.0.0
flask-cors>=4.0.0

# Notes:
# - Jupyter/notebook/matplotlib/seaborn/implicit/pyarrow/pandas are not required
#   for serving src/api.py and significantly increase memory footprint.
# - If you need training/evaluation notebooks locally, install those extras separately.
