# Runtime + local artifact rebuild dependencies.
# Keep this set compiler-free on Windows so `python -m pip install -r requirements.txt`
# works with the local recommender API and training script.
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
flask>=3.0.0
flask-cors>=4.0.0
